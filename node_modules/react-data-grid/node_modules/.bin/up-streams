#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../up/bin/up-streams" "$@"
  ret=$?
else 
  node  "$basedir/../up/bin/up-streams" "$@"
  ret=$?
fi
exit $ret
